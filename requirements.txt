# Deployment requirements for the Streamlit Community dashboard / HF Space.
# The dashboard adds ./src to sys.path, so the frauddet package need not be installed.
streamlit>=1.50
plotly>=5.20
pandas>=2.1
numpy>=1.26
pyarrow>=15
scikit-learn>=1.7
xgboost>=2.0
joblib>=1.3
shap>=0.45
pydantic>=2.6
pydantic-settings>=2.2
pyyaml>=6
