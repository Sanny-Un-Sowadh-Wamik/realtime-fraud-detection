# Optional — the streaming demo runs fully in-process without Redis.
# Set to use a real broker (e.g. Upstash free tier) in production.
REDIS_URL=

# MLflow tracking — SQLite backend (modern default).
MLFLOW_TRACKING_URI=sqlite:///mlflow.db
